redis-haskell
=============

Haskell bindings for Redis, a fast persistent key-value store.

This library is getting towards where I want it to be, but is not quite there. Check back in a few weeks.

